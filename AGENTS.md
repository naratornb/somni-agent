# AGENTS.md — somni

## What this is

**somni** is a macOS desktop app (Electron) that runs a Jira-vocabulary kanban SDLC (Backlog / Epic / Story / Subtask — no Sprint) fused with a selectable engineering Methodology — Matt Pocock's workflow or obra/superpowers (per-repo setting, docs/adr/0002) — over a user's target repos: ideas are captured, groomed through a grill interview into Specs and tracer-bullet Stories, gated hard on readiness, then executed unattended — parallel `claude`/`agy` CLI runs in isolated git worktrees, reviewed and accepted by the user in the morning. Vocabulary: [CONTEXT.md](CONTEXT.md).

- Requirements: [design/design.md](design/design.md)
- Approved technical design: [design/architecture.md](design/architecture.md) — **source of truth**. If a decision must change, update that doc in the same PR; never silently diverge from it.

## Stack

Electron + TypeScript (strict) + React + Vite (electron-vite). No database — persistence is plain files. No new dependencies without strong justification — prefer stdlib/Node/platform features.

## Architecture rules (non-negotiable)

- All business logic lives in the **main process**. The renderer only renders stored state and IPC events — no logic.
- IPC: `ipcRenderer.invoke` for commands/CRUD; `webContents.send` push events for status changes and log lines; everything exposed through `contextBridge`.
- **`<repo>/.somni/` files are the source of truth** (layout in architecture.md §4: `roles/*.md`, `items/SOM-<n>-<slug>.md` + `.tasks.json` sidecars, `seq`, `backlog.json`, `runs/<id>/run.json` + logs + report). Write every state transition with an atomic temp-write-then-rename *before* acting on it. The in-memory scheduler is derived state, never authoritative; external file edits are picked up on refresh.
- Definitions (`roles/`, `items/`) stay separate from executions (`runs/`). Raw logs are gitignored via a somni-maintained `.somni/.gitignore`; machine-specific settings and worktrees stay in Electron `userData`.
- Run statuses: `Queued / Running / Completed / Failed / Skipped / Cancelled`; pipeline additionally `Paused`. Item statuses are the board columns (architecture.md §4); the two vocabularies never mix.
- **The Ready gate is enforced in main** (`item:setStatus`, `pipeline:add`): no Story runs without a non-empty Spec and ≥1 selected subtask. The UI hides affordances; main refuses.
- Worktrees live under `<appData>/worktrees/`; branches are named `somni/<slug>-<yyyymmdd>`.
- Rate-limit errors **pause the pipeline** with backoff — they do not consume a task's single retry.
- The orchestrator stays one small boring module: no job-queue libraries, no worker threads.
- **All runner differences (claude vs antigravity) live behind the Runner adapter** (architecture.md §5: `buildArgs`/`parseLine`). Nothing outside an adapter may branch on runner type. Execution profile `{runner, model, effort}` resolves role → repo config → global settings.

## Working style (Karpathy guidelines)

1. **Think before coding.** State assumptions explicitly. If the design doc is ambiguous, or a simpler approach exists, say so before implementing — don't pick silently.
2. **Simplicity first.** Minimum code that completes the current milestone. No speculative abstractions, no unrequested configurability, no error handling for impossible cases.
3. **Surgical changes.** Touch only what the task requires. Match existing style. Remove orphans your own change created; leave pre-existing code alone unless asked.
4. **Goal-driven execution.** Before coding, state a verifiable success criterion (e.g. M0: "click button → live claude output streams into the window → final result event parsed"). Non-trivial logic ships with one minimal check/test. Loop until verified.

## Team — multi-agent approach

Non-trivial work is executed by the agent team defined in [design/team.md](design/team.md): the main session acts as **Technical Director** (briefs, reviews, green-lights) and spawns the project agents in `.claude/agents/` — `fullstack-engineer` (implements), `tester` (verifies), `ux-designer` (UI tasks). The TD sizes the work first and staffs per the tiers in design/team.md — often a single engineer for small tasks, the full roster only when scope/risk warrants it; trivial mechanical changes need no agents at all. Agent-staffed work executes from an **Execution Brief** at `design/briefs/` (see team.md) — specialists get pointer prompts to it, and it commits with the work's PR.

## Commit messages

- Format: `<milestone or area>: <what changed>` in imperative mood, matching existing history (e.g. `M2: single workflow run in an isolated worktree`, `Design: runner abstraction`).
- Subject ≤ 72 chars; body (when needed) explains *why* and any decisions/trade-offs, not a file list.
- One logical change per commit; each commit should build and pass tests.
- **No AI attribution** — never add `Co-Authored-By: Claude`, "Generated with Claude Code", or similar trailers.

## Commands

```
npm run dev     # launch app in dev mode (HMR)
npm run build   # typecheck + production build
npm test        # vitest unit checks
npm run lint    # eslint
```

## Status & roadmap

**Current milestone: M19 done — the Turn module** (`src/main/turn.ts`, term in CONTEXT.md: one prompt→reply exchange with a Runner — exactly one attempt, always time-bounded, always cancellable. Deepens the old runnerText/spawnRunner scatter into one seam: `turn(req, {signal}) → TurnResult` where req = prompt/settings/cwd + optional runner/model/effort overrides, resumeSessionId, readOnly/autonomous, timeoutMs (default settings.timeoutMinutes → 30m), graceMs, logPath, onText/onSession/onStderr; result = `{ok, text, sessionId?, exitCode, usage}` | `{ok:false, kind: spawn|exit|timeout|aborted, detail, rateLimited, ...}` — rate-limit classification INSIDE the seam via runner.isRateLimit. All six call sites migrated: executor subtask loop + auxTask (Ctrl.handle → Ctrl.ac AbortController; **fixes the bug where cancelPipeline couldn't kill Review/Fix turns and aux turns ran unbounded** — aux turns now share the run's signal + timeout), chat sendChat (inFlight holds AbortControllers; turnArgs survives only for chat.test's argv contract, ponytail-marked for deletion), report compact+full turns, repoIpc field:refine, Playground runTask (moved runner.ts → turn.ts with killTask/wireTaskIpc; runner.ts is pure spawn plumbing again). runnerText DELETED from report.ts (deletion test passed — nothing reappeared). Retries/MAX_ATTEMPTS/rate-limit gate stay Pipeline policy in executor. Behavior changes (grilled + agreed): every turn is time-bounded (report/refine/chat included) and killable; aux-task text falls back to streamed text when an adapter omits result detail. Executor/chat suites untouched as the regression net; 8 new turn.test.ts units (success/empty/exit/rate-limit/stderr-fallback/spawn/timeout/abort). 554 tests.) Earlier: **M18 done — default SDLC roles seeded on repo open** (`DEFAULT_ROLES` in `src/main/store.ts`: seven personas — architect, developer (pins `runner: claude`, agy can't read `.claude/skills/`), tester, reviewer, tech-writer, devops, security — seeded by `ensureSomni` via the existing `saveRole` ONLY while `.somni/roles/` has never existed, so deleting/editing a role never resurrects it and repos with roles are untouched; `repo:load` already calls `ensureSomni`, so zero new IPC/UI. Preambles are personas only — the discipline preambles own TDD/scope. `applyProposal`'s existing-slug-wins rule already protects them from AI overwrite. Tests in store.test.ts + a repo:load seeding assertion in repoIpc.test.ts.) Earlier: **M17 done — selectable Methodology (Pocock / Superpowers)** (governing ADR: `docs/adr/0002-methodology-neutral-items.md`. `Settings.methodology: 'pocock'|'superpowers'` (default pocock; global setting + `.somni/config.json` repo override through `resolveSettings`, selects in SettingsView global form + RepoSection). Items/Ready gate/Interview UI/`somni-*` fences are methodology-NEUTRAL — no per-item stamp, the repo's current setting governs every groom and run. Swap surface: `groomPreamble(roleSlugs, context?, methodology)` picks a charter variant from `CHARTERS` in chat.ts (fences + `somni-groomed` schema invariant); executor's `REVIEW_PROMPT`/`FIX_PROMPT` take methodology (superpowers names `requesting-code-review`/`systematic-debugging`). Skills tree is methodology-keyed: `resources/skills/<methodology>/manifest.json` — pocock's 11 dirs unchanged, superpowers vendors 8 pinned dirs from obra/superpowers (brainstorming, writing-plans, executing-plans, subagent-driven-development, test-driven-development, systematic-debugging, verification-before-completion, requesting-code-review; upstream has no separate testing-antipatterns — it lives inside test-driven-development). `skills.ts` takes methodology everywhere; the `.somni-skills.json` marker records `{version, methodology, dirs}` so `skillsStatus` reports the other methodology as not-set-up and `injectSkills` removes the previous marker-recorded dirs on switch (legacy version-only markers read as pocock); `wireSkillsIpc(methodologyOf)` gets the resolver injected from index.ts. Execution fork: pocock unchanged (one process per subtask); superpowers = ONE process per Story — `runStory`/`execute` collapse the run to a single synthetic `PLAN_TASK_TITLE` task whose prompt is `storyPlanPrompt(specPath, defs, roles)` (`PLAN_PREAMBLE` discipline + ordered `## Step n` list with role personas inline), so MAX_ATTEMPTS retries and the review loop apply at Story level and a cross-methodology resume fails the story-changed check honestly; profile resolves from settings (per-subtask role profiles don't apply). The review loop remains the final Story-level gate in BOTH modes. 542 tests.) Earlier: **M16 done — vendored Pocock skills + the implement discipline; PHASE 3 COMPLETE (M13–M16)** (brief: `design/briefs/M16.md`. `resources/skills/` vendors 11 pinned skill dirs (grilling, grill-with-docs, domain-modeling, to-spec, to-tickets, implement, tdd, code-review, research, diagnosing-bugs, resolving-merge-conflicts) + `manifest.json` `{version, upstream, skills: {name: skillFolderHash}}` — every hash pinned from `~/.agents/.skill-lock.json`; `electron-builder.yml` `extraResources` keeps them readable outside the asar (`bundledDir()`: `process.resourcesPath` packaged / app path dev). `src/main/skills.ts`: `skillsStatus`/`injectSkills`/`wireSkillsIpc` — write-set is EXACTLY manifest-listed dirs under `<repo>/.claude/skills/` + the `.somni-skills.json` version marker + `docs/agents/issue-tracker.md` (template declaring `.somni/items/` as the local tracker: SOM-n ids, frontmatter statuses, blockedBy) + `mkdir docs/adr/` + `CONTEXT.md` only-if-absent; never deletes, never touches user files (`custom/` skills proven untouched); dismissible set-up/upgrade banner in App + Settings "This repo" row (checkCommand field + skills status/action). Executor: `DISCIPLINE_PREAMBLE`+`subtaskPrompt` prepends discipline (names the story's spec file, instructs the injected implement skill's TDD) ahead of the role preamble on every subtask argv; after the last subtask `reviewLoop` (aux-task mechanism per report.ts's precedent) optionally runs `checkCommand` (`/bin/sh -c` in the worktree, task-timeout idiom — arbitrary shell, §10-noted, same trust boundary as autonomous tasks) then an auto-appended Review task (diff vs baseSha + tests → fenced `somni-verdict` parsed via `lastBlock`); RED = checkCommand fail OR verdict red, missing/malformed verdict = RED unless checkCommand passed (conservative green — "green" never means "the parser shrugged"); red appends Address-findings + fresh Review, MAX 2 fix cycles → Failed → needs-attention; green → Completed → review. `ReviewCycle`s + aux flags persist in run.json (aux tasks carry attempts/costs; excluded from the resume story-changed check — resume restarts the loop at cycle 1); `report.md` renders per-cycle verdicts incl. the plain "agent said so" caveat when neither signal is deterministic; fixtures emit green verdicts by default with `FAKE_VERDICT` overrides. `config:get/set` IPC for repo-level `.somni/config.json` (checkCommand lives there only). 538 tests; banner-variant renderer state is manually walked (effect-driven, SSR limit).) Earlier: **M15 done — capture + command palette** (brief: `design/briefs/M15.md`. Friction-free PO intake, ZERO new IPC channels — capture is the existing `item:save` create path (M13's backlog append covers ordering): `captureItem` in `ui.ts` is the single literal builder (first line → name, rest → spec, kind idea, status backlog) and `saveCapture` the single call (empty → silent noop); three surfaces share it — `CaptureModal` (`capture.tsx`: overlay + 480px panel, 4-row prose textarea, MicButton, [Groom now →]/[Add to Backlog], Enter/Cmd+Enter saves + clears + REFOCUSES with the modal staying open for rapid-fire, Esc/backdrop closes silently, Groom-now routes the new id through App's groom handoff), `QuickAdd` (pinned atop the Backlog column, "+ Add idea…" expands on focus, collapses blur-if-empty), and `CommandPalette` (Cmd+K, z-40 above the modal's z-30, backdrop + Esc close; `paletteResults` ranks commands before case-insensitive id/name item hits; actions: Capture as idea, open item → StoryPanel, Go to view over the PO-filtered nav list, Run pipeline = PipelineView's drain action). Header "+" (add glyph) in every view, disabled without a repo; Cmd+N opens capture; keydown wiring in App.tsx's mount effect; Esc closes top-most only. Backlog drag-to-reorder landed (M13 deferral): intra-column drop → `reorderBacklog` → `backlog:set` full-array write with stopPropagation, cross-column drops keep M13 setItemStatus semantics. NO OS-level global shortcut (deferred to Settings, later). 522 tests incl. the full-chain e2e capture → groom → drain → review; DnD/keydown remain documented SSR-harness gaps verified by code reading + TD live pass.) Earlier: **M14 done — Grooming** (brief: `design/briefs/M14.md`; architecture.md §7 rewritten for grooming. The M8 interview machinery re-aimed, not rebuilt: `groomPreamble(roleSlugs, context?)` in `chat.ts` keeps the `somni-question` Interview + Propose Now verbatim and adds the grooming charter (altitude choice: big intent → Epic of vertical-slice Stories with blocking edges, small → one Story; Specs with verifiable success criteria; subtask prompts are goals) plus the fenced `somni-groomed` schema `{kind, name, spec, stories?, subtasks?, roles?}` where child-story `blockedBy` is ZERO-BASED INDICES OF EARLIER ENTRIES only (forward/self/out-of-range/non-integer rejects the whole proposal — M8 invalid-role precedent). `applyProposal(repo, key, proposal)` v2: converts the groomed item in place keeping id/created (rename moves the file via `saveItem`), idea→epic lands `backlog` / story lands `ready`, children created sequentially with `epic` field + resolved blockedBy ids + sidecars all `ready`, existing role slug always wins, `_draft` groom appends root to `backlog.json` and renames the transcript to `<rootId>.jsonl`. `sendChat` seeds item-keyed turn-1 with `# name\n\nspec` and flips status to `grooming` per-turn (self-heals mid-interview drags). UI: DraftView → `GroomView.tsx` (nav "Groom", `chat_bubble` glyph — already subsetted; in `PO_VIEWS`), Board's `Groom →` + Grooming-column card clicks open it, Needs-Attention gains Re-groom; StoryPanel stays the hand-edit surface elsewhere; DraftChatPanel deleted (orphan since WorkflowsView died — REFINE_STRUCTURE with it); `ProposalPreview` renders kind/spec/child-story cards with blocked-by names. Retired: `Workflow` type, `loadWorkflows`/`saveWorkflow`/`deleteWorkflow`/`loadBrief`, the `somni-workflow` block. Epics are not re-groomable from cards (child-reconciliation semantics deferred). 510 tests incl. real-applyProposal e2e: epic proposal → Apply → blocked children drain in dependency order.) Earlier: **M13 done — work-item model v2 + the Board** (briefs: `design/briefs/M13.md` / `M13-ui.md`; Phase 3 plan in [design/architecture.md §9](design/architecture.md), vocabulary in [CONTEXT.md](CONTEXT.md). The clean break: `src/main/store.ts` v2 item store — `items/SOM-<n>-<slug>.md` (hand-rolled flat `key: value` frontmatter: id/kind idea|epic|story/status backlog|grooming|ready|in-progress|needs-attention|review|done/epic?/blockedBy?/created; body = H1 name + Spec) + `.tasks.json` sidecar for stories (the v1 task array shape verbatim), `seq` bare-integer id counter with a max-over-existing-files reuse guard (a failed save may burn an id — accepted), `backlog.json` repurposed as Backlog-column ordering (main appends on backlog-status create only), v1 `workflows/` silently ignored (`loadWorkflows` survives solely for chat.ts's applyProposal, retires M14). **Status is the tick**: `pipeline:add`/`item:setStatus` gate through `readyBlocker` (story kind + non-empty spec + ≥1 selected subtask — kind checked in main AND in the drain's `next()`, since .somni/ is hand-editable), passing writes `in-progress` + `wakeDrain()`; `next()` scans stories by status skipping active ids and unmet `blockedBy`; `execute`'s finally transitions Completed→`review`, Failed→`needs-attention`, Cancelled→`ready` (fail-soft). `run.json`'s `workflow` key is frozen carrying the story id — old runs still render. UI: `BoardView.tsx` home (seven fixed columns, per-column sticky headers + independent scroll, board scrolls horizontally; HTML5 DnD with NO optimistic move — a refused drop flashes `ring-error` + surfaces main's error verbatim in an `ERROR_BANNER`; In Progress/Done not draggable; per-column affordances Groom→/Add to pipeline/Re-run/Accept; `KIND_CHIP`+`CHIP_SM` atoms added to ui.ts on the STATUS_CHIP formula), `StoryPanel.tsx` full-content swap (text-only "‹ Back to Board" — icon font untouched; Spec textarea with non-blocking gate hint; subtask editor is WorkflowsView's task-row block renamed s/task/subtask/; Epics get a read-only child-story list; Ideas spec-only, kind never auto-promoted); WorkflowsView deleted, Draft view hidden until it returns as Grooming (M14), PipelineView is the live-execution dashboard only (Board owns waiting-work visibility), `PO_VIEWS = Board/Pipeline/Runs`. 506 tests incl. module-level e2e idea→refusal→ready→drain→review/needs-attention.) Earlier: **M12 — voice input; Phase 2 complete** (brief: `design/briefs/M12.md`, governing [ADR 0001](docs/adr/0001-in-app-whisper-voice-input.md). `src/main/voice.ts` owns the whole voice surface: `wavFromPcm` (pure 44-byte-header 16-bit mono WAV encoder incl. the float→Int16 clamp), a session-cached `--help` probe of the **brew-installed `whisper-cli`** (new `whisperBinary` setting, empty = PATH — the claudeBinary pattern; never app-distributed, no official macOS artifact exists), `voice:status` `{binary, model}`, `voice:downloadModel` (streams `ggml-base.en.bin` ~142 MiB from HF to `userData/models/` via fetch → `.tmp` → rename, per-MiB `voice:modelProgress` pushes, promise-deduped at the handler so concurrent clicks share one fetch), `voice:transcribe(Float32Array)` (fixed tmp WAV + module-level in-flight guard doubling as cross-field mic exclusivity, `whisper-cli -m <model> -f <wav> -np -nt` — flags pinned live, stdout.trim() is the transcript, 60s timeout, finally-cleanup). Renderer capture is deliberately tiny in `chatShared.tsx`: getUserMedia → 16 kHz AudioContext → ScriptProcessorNode (`ponytail:` AudioWorklet upgrade path) → one Float32Array over invoke; `MicButton({onText})` states checking/no-binary(+brew hint)/no-model/downloading %/idle/recording/busy, append-to-end insertion, mic released on unmount; four call sites (DraftView, DraftChatPanel, WorkflowsView task prompt, RolesView preamble — the latter two beside RefineControl). macOS dictation stays the zero-code fallback; `com.apple.security.device.audio-input` added to entitlements; getUserMedia is the TCC prompt (no askForMediaAccess). **Material subset regenerated** for `mic` — the procedure note in main.css now names both glyph sources (literal spans + the App.tsx VIEWS map `{icon}` path) and pins verification to cmap+GSUB ligature reconstruction, after a regeneration that greppped only literals dropped the seven sidebar glyphs (caught in TD review). Tests (`voice.test.ts` + SSR): encoder bytes/clamp, transcribe argv + temp lifecycle + guard, status branches (missing-binary via fresh module import + wiped PATH), model-presence-no-fetch; live pass verified the full first-use flow incl. a real download (140 progress events) and a real `say`-synthesized "quick brown fox" round trip through whisper. English-only until the model choice is revisited (ADR).) Earlier: M11 Refine, model lists, view modes (briefs: `design/briefs/M11.md` / `M11-ui.md`. **Refine** (§9, one-shot): `runnerText` exported from `report.ts`; IPC `field:refine(repo, kind, text)` in `repoIpc.ts` — kind-specific prompt consts (task prompt / role preamble), global-settings profile, read-only, empty text refused, stateless (no in-flight guard); shared `RefineControl` in `chatShared.tsx` under the task-prompt and role-preamble textareas — inert result card, Apply writes the **editing buffer only** (CONTEXT.md Apply rule), disk only via the editor's Save. Workflow-structure refine = canned `REFINE_STRUCTURE` const à la `PROPOSE_NOW` (chat.ts + preload data prop `refineStructure`, ordinary `chat:send` from a DraftChatPanel header button; zero new IPC), enabled by `draftPreamble(roleSlugs, brief?, slug?)` naming `.somni/workflows/<slug>.json` for editor chats (slug threaded through `turnArgs`/`sendChat`; `_draft` passes undefined). **Model lists**: `Runner.listModels(binary)` — claude resolves curated `['fable','opus','sonnet']` (its CLI has no models subcommand), agy runs `execFile(binary, ['models'], {timeout: 10_000})` and tab-parses `id\tLabel` stdout, any error/empty ⇒ pinned 5-id fallback (`ponytail:` in runners.ts); IPC `models:list(runnerName?)` resolves the inherit case in main (`undefined → settings.runner → claude`) and memoizes the *promise* per `name:binary` for the app session (failed query serves fallback until restart — accepted); model fields in Settings/Roles are native `<input list>`+`<datalist>` combos, free text stays legal. **View modes**: `ViewMode = 'po'|'engineer'`, `Settings.viewMode` (default engineer in `SETTINGS_DEFAULTS`), persisted via `settings:set` (preload `setSettings` widened to `Partial<Settings>`); App.tsx filters the nav to `PO_VIEWS = Draft/Workflows/Pipeline/Runs` in PO (queueing lives in WorkflowsView), sidebar-bottom "PO | Engineer" segmented toggle reusing the selected-nav-row classes, hidden current view falls back to Workflows, presentation only — no permissions, no other behavior change. Tested on the existing harnesses (149 tests: adapter listModels incl. timeout assertion, `field:refine` read-only argv invariant, models:list memo proof, preamble slug line) plus a TD live-app pass.) Earlier: M10 whole-app UI redesign + Runs & Reports (Nocturnal Mission Control design system from `design/stitch_somni_ai_orchestrator/` (mocks + DESIGN.md, committed as the design source of truth): Tailwind v4 renderer-only via `@tailwindcss/vite`, every DESIGN.md token declared CSS-first in `src/renderer/src/assets/main.css` `@theme` (names mirror the mocks' inline config so mock markup ports verbatim; where DESIGN.md prose and mocks disagree — radii, pill buttons — the mocks win), dark-only, no `dark:` variants; fonts bundled locally (Inter + JetBrains Mono variable woff2, Material Symbols subset to the glyphs in use — adding a glyph means regenerating the subset, `ponytail:` comment in main.css); 240px sidebar shell in `App.tsx`, shared atoms as exported class strings in `src/renderer/src/ui.ts` (+ `statusChip()`), all seven views restyled (four from mocks, four from the accepted spec `design/briefs/M10-ui.md`), legacy hand-written CSS deleted, SSR render smoke test in `views.test.tsx`. Briefs: `design/briefs/M10.md` / `M10-ui.md`. **Runs & Reports** (`design/briefs/runs-reports.md`): both runner `parseLine`s now extract token usage (claude `input+cache_creation+cache_read`/`output`; agy `input+cache_read`/`output`, thinking already inside output — verified live) onto `TaskRun.promptTokens/completionTokens`; `RunState.stats` (per-file numstat⨝name-status `{path, kind, lines}` + totals) persisted into run.json at report time; IPC `runs:details` (persisted stats → live compute while the worktree exists → null; carries `branchExists`), `runs:switchBranch` (dirty-tree and missing-branch guards via `lockedGit`; the UI disables Switch while the run's worktree holds the branch — git refuses that switch), `runs:revealWorktree`; RunsView rebuilt to the mock — metric tiles (`1m 39s`/`$0.04`/`12.4k`, em-dash when absent), Summary panel (`## Summary` extracted from report.md; minimal style shows a hint), Files Changed `+`/`~` list, collapsed task table, raw report pane dropped; `repoIpc.test.ts` covers the handlers with an electron-module mock over real git repos.) Earlier: M9 Backlog & drain (§3): the pipeline is now a drain — `drainLoop` in `src/main/executor.ts` replaced the fixed-set worker pool with one supervisor loop that refills up to `maxConcurrency` from a `next()` callback, races in-flight jobs against `sleepOrWake(pollMs=2000)` (woken instantly by `wakeDrain()` from tick/promote/toggle IPC), and stops when nothing is in flight and it isn't keep-running; `startDrain` supplies the scanning `next` (re-reads `loadRepo` per refill, alphabetical slug order, skips `activeSlugs` so a re-tick runs *after* never concurrently, **consumes the tick via `store.setSelected` before the spawn**, untick failure → per-drain skip set + continue to the next candidate); `resumePipeline` is the same loop over a fixed set, mode `'resume'`, never scans. `DrainMode = manual|nightly|keep|resume`; `getDrainState()` + `pipeline:state` invoke; `pipeline:status` push carries `mode` (null on final Idle), Running/Idle deduped, Paused re-emits fresh `resumeAt`; `setKeepRunning` flips the stop rule in place (module flag, never persisted, cancel clears it; toggle-off sets `stopping` — finish in-flight, pick up nothing). Nightly Window: `nightlyTime`/`nightlyArmed` in global settings (armed survives restart, time survives disarm), `armNightly`/`fireNightly` in `index.ts` (pure `msUntil` in executor; disarm written before draining `lastRepo`; re-armed via `wireRepoIpc(onSettingsChanged)`); `powerSaveBlocker` now follows status pushes (held Running/Paused, released Idle). Backlog: `.somni/backlog.json` ordered slug array (`loadBacklog` prunes missing in memory), IPC `backlog:set`/`backlog:park` (untick+append)/`backlog:promote` (remove+tick+wake); `pipeline:start(repo, slugs)` = tick named slugs + wake or start a manual drain (PipelineView sends `[]`). UI: Workflows gains the ordered Backlog section (ordinal, ↑/↓, Promote; parked rows excluded from the main list, no tick) and per-row "To backlog"; Pipeline shows "▶ Drain queue", the Keep Running checkbox, mode-labeled chips ("Draining — waiting for work" when idling) and latest-run-wins cards; Settings gains the Nightly row (time + Armed/Disarmed chip). Briefs: `design/briefs/M9.md` / `M9-ui.md`.) Earlier: M8 Brief-first drafting (§7): the Interview discipline lives in `draftPreamble` (one fenced ```` ```somni-question ```` block per reply — `{question, options, recommended}`, parsed by `parseQuestion` beside the shared `lastBlock` fence helper, malformed blocks degrade to plain text) and the proposal widened to `{name, brief, tasks, roles}` (`parseRoles`: one invalid role rejects the whole proposal); `applyProposal` in `chat.ts` (IPC `proposal:apply`) is now the single mutation out of any chat — writes only new-slug roles (existing role always wins), writes the Brief sidecar `workflows/<slug>.brief.md` (absent brief leaves it alone), draft Apply creates a ticked workflow with a uniquified slug and renames `chats/_draft.jsonl` → `chats/<slug>.jsonl`, editor Apply preserves the tick, refused while that key's turn is in flight; `deleteWorkflow` removes json + brief + transcript (a reused slug must not inherit a dead session); the chat guard is per-workflow via the executor's `activeSlugs` (`isRunning(slug)`, resume-aware) so `_draft` and idle workflows chat during a pipeline; `ChatEvent.done` carries the parsed `question`; editor chats prepend the stored Brief to turn-1 context. UI: **Draft** sidebar view (`DraftView.tsx`, full-page `_draft` interview with always-visible Propose Now — the fixed transcript-visible `PROPOSE_NOW` message), shared `chatShared.tsx` (`QuestionCard` with recommended-highlighted chips that send on click, `ProposalPreview` with collapsed Brief, task cards, new-role cards incl. the "already exists — will reuse" badge), consume-once `openSlug` handoff into the workflow editor after Apply, and a read-only collapsed Brief `<details>` in the editor. Execution brief + accepted UI spec: `design/briefs/M8.md` / `M8-ui.md`.) Earlier: M7 Runner adapter + Antigravity (§5): `src/main/runners.ts` holds the whole runner surface — `Runner {name, binary, binarySetting, buildArgs(prompt, {model, effort, resumeSessionId, readOnly, autonomous}), parseLine, isRateLimit}` — and `getRunner(name, settings)` is the only name→adapter map (unknown name falls back to claude; `settings.claudeBinary`/`antigravityBinary` override the binary, empty = PATH). `claudeRunner` folds in the old executor/`turnArgs`/report argv plus the ex-`stream.ts` parser and the ex-executor RATE_LIMIT regex; `antigravityRunner` is `agy -p --output-format stream-json` with `--mode plan` (read-only), `--dangerously-skip-permissions` (autonomous), `--conversation <id>` (resume), `--model`/`--effort`, parsing agy's `{event: init|step_update|result}` envelope (no cost — agy reports tokens only) and Google's quota/RESOURCE_EXHAUSTED wording; all flags pinned against the installed CLI, not guessed. `stream.ts` is now just `feed(buffer, chunk, parseLine)`; `runner.ts` exposes `spawnRunner(runner, args, …)`; executor/chat/report/Playground are runner-agnostic and the rate-limit pause asks `runner.isRateLimit`. Profile gains `runner` (role frontmatter `runner:` → `.somni/config.json` → global, default claude) via `resolveProfile`; `TaskRun.runner` records it, resolved once per task so a retry can never switch runners. Settings view: runner dropdown + per-runner binary paths (ponytail: one free-text model field, no per-runner model lists); role editor: runner select (inherit). Both CLIs support resume, so §5's chat→ClaudeRunner fallback was unnecessary. Earlier: M6 AI workflow drafting — "Draft with AI" (§7): `src/main/chat.ts` runs one read-only chat session per workflow slug over the existing `spawnClaude` path (`--allowedTools "Read,Glob,Grep"`, never `--dangerously-skip-permissions`, cwd = the repo, `--resume <sessionId>` from turn two, repo→global `--model`/`--effort`); a fixed drafting preamble listing the repo's role slugs is prepended to the first turn; transcripts append to `.somni/chats/<slug>.jsonl` (message lines + sessionId, "New chat" clears both); `parseProposal` extracts the last ```` ```somni-workflow ```` block (closing fence anchored to line start) and validates it; IPC `chat:load`/`chat:send`/`chat:new` with `chat:event` push (text/done/error), one in-flight turn per slug, refused while a pipeline runs, killed on before-quit. `DraftChatPanel` is a 340px column in the workflow editor: streaming bubbles, pinned proposal preview with Apply/Dismiss — Apply is the only write and goes through the existing `workflow:save`. Earlier: M5 reports, settings, run history, worktree cleanup — global settings in userData `settings.json` (`concurrency`/`timeoutMinutes`/`reportStyle`/`model`/`effort`) with per-repo `.somni/config.json` overrides and role `---model:/effort:---` frontmatter, resolved role → repo → global in `src/main/store.ts` (`resolveSettings`/`resolveProfile`) and applied per task in the executor (`--model`/`--effort` on the spawn, recorded on each TaskRun); `src/main/report.ts` writes `runs/<id>/report.md` on Completed *and* Failed runs — minimal (git diff vs the run's `baseSha`, created/modified counts, test files, per-task status/duration/cost), compact (+ one read-only `claude -p` summary), full (+ an autonomous Report task appended to run.json); Runs & Reports view (`runs:list`/`runs:report`) and `runs:cleanup` (`git worktree remove` + `git branch -d`, errors surfaced, never forced); Settings view. Earlier: M4 unattended reliability — retry-once-then-halt, per-task timeout SIGTERM→SIGKILL, rate-limit pipeline pause with backoff, crash resume, powerSaveBlocker. M3 pipeline — FIFO worker pool, serialized `git worktree add`, PipelineView dashboard.) Phase-1 milestones M0–M7 and Phase-2 milestones M8–M12 are complete. Phased plan is in [design/architecture.md §9](design/architecture.md). When a milestone lands, update this line and the Commands section.

## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues on naratornb/somni-agent via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root plus `docs/adr/`. See `docs/agents/domain.md`.
